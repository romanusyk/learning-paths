[project]
name = "learning-paths"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "matplotlib>=3.8",
    "jupyter>=1.0",
    "pyyaml>=6.0",
]
